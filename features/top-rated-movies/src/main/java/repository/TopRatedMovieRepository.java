package repository;

import com.egdroid.mapper.MovieMapper;

public class TopRatedMovieRepository {

    MovieMapper movieMapper =  new MovieMapper();

 /*   public MovieDataSource getMovies(){

// mwgoda fe el localmodule w e7na 3amlinha hnak api 3lshan keda mwgoda
// room
        MovieLocal --> getMovies()
// retrofit
        MovieResponse movieRemote = new MovieRemote.getMovies()

        if( MovieLocal != null )
        { return movieMapper.mapToDataSource(MovieLocal);
        }else
return movieMapper.mapToDataSource(MovieResponse

        }

    }*/

}
